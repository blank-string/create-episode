// change audio times, start, end by sync amount
// trim to the sync amount
// pick shortest start and longest end, else start = 0 end = longest
// change all audio times and end by start
// trim to start and longest end
